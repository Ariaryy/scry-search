//! SDK for talking to a running `scryd` daemon over its named pipe.
//! Placeholder — see task #6.

pub struct Client;

impl Client {
    pub fn connect() -> anyhow::Result<Self> {
        anyhow::bail!("scry-client: not yet implemented")
    }
}
