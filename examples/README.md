# Examples

Run the small persistent-client example while an elevated `scryd` is running:

```text
cargo run -p scry-example --release
```

Each input line is submitted through one reusable search session. Try
`report type:file ext:pdf` or `type:dir`.

