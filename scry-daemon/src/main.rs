//! scryd: background indexing daemon. IPC server (named pipe) + Arena owner.
//! Currently a placeholder entrypoint — see task #5 for the named pipe server.

fn main() -> anyhow::Result<()> {
    println!("scryd: not yet implemented");
    Ok(())
}
